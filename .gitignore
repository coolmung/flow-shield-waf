# env / secrets
.env
*.local
docker-compose.override.yml
docker-compose.extra-ports.yml

# python
backend/build/
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# node
node_modules/
dist/
.vite/
*.log

# data volumes (if bind-mounted locally; keep root-only so backend/app/data seeds stay tracked)
/data/
/backup/
engine/conf.d/*.conf
!engine/conf.d/.gitkeep

# codegraph (local index, per-machine)
.codegraph/

# editor / local tooling
.cursor/
*.code-workspace
*.tsbuildinfo

# os
.DS_Store
